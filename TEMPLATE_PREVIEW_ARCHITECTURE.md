# Template Preview Architecture - Visual Guide 🎨

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     RESUME BUILDER                          │
│                  (resume-builder.html)                       │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ User clicks "Preview"
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              previewTemplate(templateId)                     │
│                                                              │
│  1. Show modal with loading spinner                         │
│  2. Prepare sample data                                     │
│  3. Fetch from backend API                                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ POST /preview-resume
                            │ with sample data
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   BACKEND SERVER                             │
│                     (main.js)                                │
│                                                              │
│  app.post('/preview-resume', async (req, res) => {         │
│    const template = req.body.template;                      │
│    const html = generateTemplateHTML(template, data);       │
│    res.send(html);                                          │
│  })                                                          │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ Calls template generator
                            ▼
┌─────────────────────────────────────────────────────────────┐
│          generateTemplateHTML(template, data)                │
│                                                              │
│  • generateClassicTemplate()                                │
│  • generateMinimalTemplate()                                │
│  • generateModernProTemplate()                              │
│  • generateTechFocusTemplate()                              │
│  • generateCreativeBoldTemplate()                           │
│  • ... and 5 more templates                                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ Returns HTML string
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  RESPONSE TO FRONTEND                        │
│                                                              │
│  <!DOCTYPE html>                                            │
│  <html>                                                     │
│    <head>                                                   │
│      <style>/* Template-specific styles */</style>         │
│    </head>                                                  │
│    <body>                                                   │
│      <!-- Rendered resume with sample data -->             │
│    </body>                                                  │
│  </html>                                                    │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ Frontend receives HTML
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    RENDER IN MODAL                           │
│                                                              │
│  previewContent.innerHTML = `                               │
│    <div style="transform: scale(0.8);">                     │
│      ${previewHTML}                                         │
│    </div>                                                   │
│  `;                                                          │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ User sees preview
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                 USER INTERACTIONS                            │
│                                                              │
│  ✅ View preview with sample data                           │
│  ✅ Click "Use This Template" → Go to form                  │
│  ✅ Click × or outside → Close modal                        │
│  ✅ Try another template → Repeat process                   │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

```
USER ACTION                FRONTEND                  BACKEND
    │                         │                        │
    │ Click Preview          │                        │
    ├────────────────────────▶│                        │
    │                         │                        │
    │                         │ Show Loading...        │
    │                         │                        │
    │                         │ POST /preview-resume   │
    │                         ├───────────────────────▶│
    │                         │                        │
    │                         │                        │ Generate HTML
    │                         │                        │ with template
    │                         │                        │
    │                         │   HTML Response        │
    │                         │◀───────────────────────┤
    │                         │                        │
    │                         │ Render in Modal        │
    │                         │                        │
    │ See Preview            │                        │
    │◀────────────────────────┤                        │
    │                         │                        │
```

## Before vs After Comparison

### ❌ BEFORE (Hardcoded)
```
┌──────────────────┐
│   Frontend       │
│                  │
│ • Template Styles│
│ • HTML Generator │
│ • Preview Logic  │
└──────────────────┘
        ↓
   Inconsistent!
   Different from PDF
```

### ✅ AFTER (Backend-Driven)
```
┌──────────────┐         ┌──────────────┐
│  Frontend    │────────▶│   Backend    │
│              │  Fetch  │              │
│ • Show Modal │ Preview │ • Templates  │
│ • Render     │◀────────│ • Generator  │
└──────────────┘   HTML  └──────────────┘
                              ↓
                      Used for Preview
                           AND PDF
                              ↓
                        Consistent! ✅
```

## Component Breakdown

### Frontend Components

```
┌─────────────────────────────────────┐
│      Template Selection Grid         │
│                                      │
│  ┌────┐  ┌────┐  ┌────┐  ┌────┐   │
│  │ 📄 │  │ 📄 │  │ 📄 │  │ 📄 │   │
│  └────┘  └────┘  └────┘  └────┘   │
│   ↓ Click Preview                  │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│         Preview Modal                │
│                                      │
│  ┌─────────────────────────────┐   │
│  │  Template Name              │×  │
│  │  Description                │   │
│  ├─────────────────────────────┤   │
│  │                             │   │
│  │  ┌─────────────────────┐   │   │
│  │  │                     │   │   │
│  │  │   BACKEND-GENERATED │   │   │
│  │  │   TEMPLATE PREVIEW  │   │   │
│  │  │                     │   │   │
│  │  └─────────────────────┘   │   │
│  │                             │   │
│  ├─────────────────────────────┤   │
│  │  [Use This Template]        │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

### Backend Components

```
┌────────────────────────────────────────┐
│          main.js Structure             │
│                                        │
│  app.post('/preview-resume')          │
│       │                                │
│       ├─▶ generateTemplateHTML()      │
│       │        │                       │
│       │        ├─▶ generateClassic()  │
│       │        ├─▶ generateMinimal()  │
│       │        ├─▶ generateModernPro()│
│       │        ├─▶ generateTechFocus()│
│       │        └─▶ ... 6 more         │
│       │                                │
│       └─▶ return HTML string          │
└────────────────────────────────────────┘
```

## Error Handling Flow

```
┌─────────────────┐
│ Fetch Preview   │
└────────┬────────┘
         │
    ┌────▼────┐
    │ Success?│
    └────┬────┘
         │
    Yes  │  No
    ┌────▼────┐    ┌─────────────────┐
    │ Render  │    │ Show Error Icon │
    │ Preview │    │ "Failed to load"│
    └─────────┘    │ Can still select│
                   └─────────────────┘
```

## State Management

```
┌────────────────────────────────────┐
│         Application State          │
│                                    │
│  • currentStep: 1 (template) or 2 │
│  • selectedTemplate: null          │
│  • modalOpen: false                │
│  • previewLoading: false           │
│  • previewError: null              │
└────────────────────────────────────┘
         │
         ▼
    User Actions
         │
    ┌────▼─────┬────────┬───────────┐
    │          │        │           │
  Click    Select   Close     Error
  Preview  Template Modal   Occurs
    │          │        │           │
    ▼          ▼        ▼           ▼
  modalOpen  Save     Close     Show
  = true     to       modal     error
  Loading... storage            msg
```

## API Contract

### Request Schema
```json
{
  "template": "modern-pro",
  "personalInfo": {
    "fullName": "string",
    "email": "string",
    "phone": "string"
  },
  "address": "string",
  "linkedin": "string",
  "summary": "string",
  "experience": [
    {
      "jobTitle": "string",
      "company": "string",
      "startDate": "string",
      "endDate": "string",
      "description": "string"
    }
  ],
  "education": [...],
  "technicalSkills": "string",
  "softSkills": "string",
  "projects": "string",
  "certifications": "string"
}
```

### Response Schema
```
Content-Type: text/html

<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <style>/* Template styles */</style>
  </head>
  <body>
    <!-- Resume content -->
  </body>
</html>
```

## Performance Metrics

```
┌──────────────────────────────────────┐
│        Performance Profile           │
├──────────────────────────────────────┤
│ API Call Time:       100-200ms      │
│ Modal Render:        50ms           │
│ Total Preview Load:  150-250ms      │
│ Template Switch:     ~200ms         │
│ Error Recovery:      Instant        │
└──────────────────────────────────────┘
```

## Browser Compatibility

```
┌─────────────────────────────────────┐
│     Supported Browsers              │
├─────────────────────────────────────┤
│ ✅ Chrome 90+                       │
│ ✅ Firefox 88+                      │
│ ✅ Safari 14+                       │
│ ✅ Edge 90+                         │
│ ✅ Mobile browsers (iOS/Android)   │
└─────────────────────────────────────┘
```

---

This visual guide helps understand:
- How components interact
- Data flow between frontend and backend
- Error handling paths
- User interaction points
- System architecture
